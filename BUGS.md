# Anchor demo — bug list and development cycle

Measured, not guessed. Every entry says how it was found and how it was verified. The point of
this file is that "it works" and "it is broken on basic questions" were both true at once for days,
because nobody had ever run the thing against questions it had not been shown.

**Harness:** `zoo_harness.mjs` loads the REAL functions out of `index.html` (no reimplementation, no
second copy to drift), stubs the browser, and scores an uncurated question zoo generated by
`demo_probe.py` sampling the shipped shards at random.

```
py -3.13 demo_probe.py --n 40 --seed 99 --out site/_zoo.jsonl
node --max-old-space-size=8192 zoo_harness.mjs site/_zoo.jsonl
```

## Answer rate

| run | sample | pass rate |
|---|---|---:|
| baseline | 30 questions, seed 4242 | **40.0%** |
| after entity-linking redesign | same 30 | **90.0%** |
| hold-out, never seen during fixing | 40 questions, seed 99 | **97.5%** |

---

## FIXED

### B1 — Entity linking shredded multi-word names *(16 of 18 failures)*
`findEntities` stripped stop-words **before** forming spans, then preferred whichever fragment was
best attested. Result:
```
"Tbilisi State Conservatoire" -> [Tbilisi | State | conservatoire]
"Anastasia at This Address"   -> [Anastasia | at | address]
"J. J. Johnston"              -> [J. | J. | Johnston]
```
Root cause was a fix for a different bug: the compound-vs-split rule added to stop `"car wheel"`
being read as one entity. It preferred split parts when better attested, so `Tbilisi`(many facts)
beat `Tbilisi State Conservatoire`(few). **A local fix for a rare case broke the common case.**

*Fixed:* one rule instead of a pile of special cases — **longest contiguous span wins**. Internal
function words are kept because they are part of the name; only the question's framing is trimmed,
and only from the ends. A second entity is sought in the leftover words, so two-entity questions
still work. Verified: 40% -> 90%.

### B2 — The asked-about relation was treated as part of the name
`"what country is Nirauli in"` trimmed only `what`, because the scan stops at the first non-framing
word. That left `country is` to win as a longer span than `Nirauli` — and there really is a subject
called **"Country Is"**, so it confidently answered *United States of America* for seven different
villages.

*Fixed:* the goal relation's own words are framing too, trimmed from both ends. Verified: 90% -> 97.5%.

### B3 — Harness reported 0% from its own bug
First run showed 0/30 ERROR. Cause was in the harness, not the engine: `let RELS` inside the page
script is a **lexical binding**, not a property of the vm sandbox, so `sandbox.RELS = …` created a
second invisible global and the engine kept seeing `null`. Recorded because it would have been
published as an engine failure.

*Fixed:* boot the index from inside the vm context.

### B4 — Harness OOM at 4GB
A parsed shard is roughly 10x its gzipped size and the harness cached them on top of the page's own
cache. *Fixed:* single bounded cache (24 shards, FIFO eviction).

### B5 — A path was presented as an assertion
`"is a caliper used in a car"` returned `caliper -> automotive -> automobile -> car` **with a
composed percentage**, implying a part-of relation the graph never recorded. In a graph this dense
most pairs connect within a few hops, so path-existence is nearly always true and says almost
nothing. *Fixed:* path results state that they are association, not assertion, in the result itself.

### B6 — New question mid-search did nothing
While thinking, the button was a pure STOP, so typing a new question and pressing it halted the old
search and started nothing — you had to press twice. Before that, the button was `disabled` during
a search, and **a disabled button emits no click event at all**, so the preempt path was
unreachable. *Fixed:* button stays live; a press with different text supersedes, same text stops.

### B7 — Per-subject cap deleted the demo's own headline answer
The exporter kept the first 80 facts per subject in row order, discarding 1,046,813 facts (1.58%)
while only 11,056 subjects (0.091%) ever exceeded it — including `Australia -capital-> Canberra`.
*Fixed:* cap removed, all 66,404,117 facts ship. Cost 9MB.

### B8 — Stale disclosure
The cap was removed and the page kept telling readers "each subject carries at most 80 facts".
Honest disclosure rots like any other claim. *Fixed:* copy corrected, and `audit_demo.py` now reads
`MAX_PER_SUBJECT` out of the exporter and fails the build if the page quotes a different number.

### B9 — Renderer swap took the graph model with it
An edit that replaced the 2D canvas with three.js sliced out `gClear`/`gNode`/`gEdge`/`physics`,
then `loop`/`startLoop`/`stopLoop`, then `tick`. *Fixed:* model restored; it is not the renderer.

### B10 — A 2D context permanently blocks WebGL
One leftover `getContext("2d")` line from the old renderer silently killed the entire three.js
scene — a canvas that has ever returned a 2d context can never return a webgl one.

### B12 - Completed chains were invisible in the 3D view until the very end
`n.on`/`e.on` (the "on the answer chain" role) were set only by the final replay, and only for
the single best chain. The search is DESIGNED to run long - so for its whole duration the stage
showed growth but none of the reasoning it had already finished. Reported by RJ as "the
visualization doesn't show the chains". *Fixed:* `liveAdd` marks every chain's nodes and edges
the moment it completes, and the render loop rebuilds on a role-change flag, not only when the
node count changes.

### B13 - The screen froze during fetch phases; no live narration existed
`traceFlush()` was only called inside the fact-scanning loop, so during a long stretch of
awaited shard downloads nothing painted - "it keeps working but there is no more updates after a
few seconds". The engine was working; the screen was not. *Fixed:* a 300ms interval owned by the
run flushes the trace and repaints during awaits, plus a fixed bottom narration bar: one plain
sentence about the current step, a progress bar whose fill is the position in the current hop's
lead list (and which pulses during downloads, because download time is not knowable), and live
step/chain counters. The "What just happened" panel now also states it is working, per question,
from the moment the search starts.

---

## Claude-contrast harness (added after the zoo)

`zoo_harness.mjs` chain mode runs the page's real `reason()` end to end and compares the
top-ranked chain against a frontier-model reference answer. The model is the CONTRAST, not a
component - the engine stays zero-weights. Bounded (30 fetches, depth 4) and disclosed.

First run: 0/8 - every chain started from the entity **"of the"**, which is literally a subject in
this graph (`of the -time period-> Roman Empire`). Connectives between the goal and the name were
not junk. After the junk-span guard (a span may CONTAIN function words but may not consist of
nothing else): **4/8 agree**, and the four differences are each informative:

| question | claude | engine | what it reveals |
|---|---|---|---|
| capital of Melbourne's country | Canberra | (0 chains) | harness bound artifact - works at full budget in the browser (89.7%) |
| capital of Barcelona's country | Madrid | Catalonia | **O7: inverse relations conflated** |
| capital of Munich's country | Berlin | Bavaria | O7 again |
| genre of Abbey Road's performer | rock | The Beatles | stops one hop short of the asked relation |

### B14 - O7: inverse relations conflated; and the asked-for relation was table-ordered
Two causes, one failure class (confidently wrong):
(a) `relMatches` used substring matching, so `capital of` - the INVERSE of `capital` - satisfied
the bare goal, and the engine answered Catalonia for the capital of Barcelona's country.
*Fixed:* a bare goal never matches its own `<goal> of` form.
(b) `readQuery` returned the FIRST alias in table order, so "the genre of the performer of
Abbey Road" got goal `performer` and stopped one hop short, answering a band to a genre
question. *Fixed:* the outermost ask wins - the relation the question names first is the one it
asks for (`location`/"where" excepted: a question word, not the asked-for noun).
Verified: contrast 4/8 -> 6/8 (Barcelona->Madrid, Munich->Berlin, Abbey Road walks
performer->genre and lands on "Rock and roll"; that row still strings-differs from the
reference "rock", which is a normalization artifact, not a wrong answer). Zoo held at 97.5%.
Suite floor raised: chainAgree 4 -> 6.

## OPEN


### O1 — `learn-on-abstain` has never fired *(shipping unproven)*
Built and wired: when a chain cannot reach its target, look the term up externally and re-walk.
Every test question has found its answer without it. Under rule 7 of the no-toys law this is either
demonstrated or removed. **Do not describe it as working.**

### O2 — Live UI unverified in motion
The three.js scene is verified by driving it from code (380 node objects from 190 nodes, 192 edges,
8 legend entries, no errors) but the browser pane kept disposing its render frame, so animation,
WASD flight, the supersede fix and click-to-inspect have not been watched running.

### O3 — Repo is ~1.2GB to clone
Multiple full 600MB shard revisions in history. `git clone --depth 1` is ~600MB and needs no
history rewrite; squashing halves it but is a force-push over published history. Needs a decision.

### O4 — `car` and `automobile` are separate nodes
No synonym layer, so surface-form mismatches fail. This is why the caliper question reaches
`automobile` but not `car`. Data-layer problem, not a parser one.

### O5 — Cold-cache latency
A wide search is tens of seconds because every hop is a ~1.2MB HTTP fetch. Shard-over-HTTP is the
wrong transport for search this wide; a server-side index or a one-time IndexedDB load removes it.

### O6 — Harness prints a `startLoop is not defined` tail
Cosmetic: the harness strips the renderer, and the page's autorun then calls into it. Results above
the trace are unaffected, but it should be silenced so a real failure is not mistaken for noise.
